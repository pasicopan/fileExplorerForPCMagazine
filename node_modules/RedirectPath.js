

function RedirectPath(){
    var that = this;
    that.init = function(options) {
        var path = window.prompt('请输入路径');
        window.localStorage.setItem('path',path);
        window.location.reload();
    }

}


exports.RedirectPath = RedirectPath;